#include<stdio.h>



//checking for a specific number in array src=the array, id=the numbere we re looking for
unsigned char CheckIndex (const unsigned char *src, unsigned char id);
unsigned char CheckIndexBinariSearch(const unsigned char *, unsigned char );


unsigned char CheckIndexBinariSearch(const unsigned char *src, unsigned char id)
{
    int left=0, right=14, mid;
    while(left < right)
    {
        mid=(left+right)/2;
        if(src[mid] == id)
           {
            return mid;
           }
        else if(src[mid] > id)
        {
            left=mid + 1;
        }
        else
        {
            right=mid - 1;
        }
    }
    return 255;
}

unsigned char CheckIndex (const unsigned char *src, unsigned char id)
{
    int i, k=0;
    for ( i = 0; i < 15; i++)
    {
        if(src[i] == id)
        {
            return i;
            k=1;
        }
    }
    if(k == 0)
    return 255;
    
}

const unsigned char MyArray[15]={1, 4, 11, 21, 22, 23, 24, 25, 28, 31, 31, 34, 35, 36, 37};
void main(){

    unsigned char id;

    scanf("%d", &id);
    printf("%d\n",CheckIndex(MyArray, id));
    printf("%d",CheckIndexBinariSearch(MyArray, id));

    
}